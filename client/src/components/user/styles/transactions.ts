import { CSSProperties } from "react";
export const styles: { [key: string]: CSSProperties } = {
  container: {
    maxWidth: 1350,
    margin: "0 auto",
    display: "flex",
    flexDirection: "column",
    gap: 32,
    paddingTop: 64,
    paddingBottom: 64,
  },
};
